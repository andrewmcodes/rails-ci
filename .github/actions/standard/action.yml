# name: "Run Standardrb"
# description: "Run Standardrb Lints on Ruby Code"

# runs:
#   using: "composite"
#   steps:
#     - run: bin/standardrb
#       shell: bash
#     - run: |
#         MESSAGE="Hello $INPUT_NAME"
#         echo "::set-output name=standard::$MESSAGE"
#       shell: bash

name: Standard

description: |
  This composite GitHub Action checks out the repository and runs
  the StandardRB linter.

outputs:
  success:
    description: 'Whether the StandardRB check passed'
    value: ${{ steps.status.outputs.results }}

runs:
  using: "composite"
  steps:
    - name: Run StandardRB
      shell: bash
      run: bin/standardrb
      continue-on-error: true

    - name: Output Status
      id: status
      run: echo "results=$(echo $?)" >> $GITHUB_OUTPUT
      shell: bash
