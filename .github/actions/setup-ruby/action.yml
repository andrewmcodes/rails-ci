name: "Setup Ruby"
description: "Sets up Ruby and runs bundle install"

runs:
  using: "composite"
  steps:
      - name: 💎 Setup Ruby
        uses: ruby/setup-ruby@v1
        with:
          bundler-cache: true
